typo3:
  build: docker/typo3/
  links:
    - solr
    - mysql
  volumes:
    - ./:/var/www/
  env_file:
    - docker-env.yml

nginx:
  build: docker/nginx/
  ports:
    - 8000:80
  links:
    - fpm
  volumes_from:
    - typo3
  env_file:
    - docker-env.yml

#httpd:
#  build: docker/httpd/
#  ports:
#    - 8000:80
#  links:
#    - fpm
#  volumes_from:
#    - typo3
#  env_file:
#    - docker-env.yml

fpm:
  build: docker/php-fpm/
  links:
    - solr
    - mysql
  volumes_from:
    - typo3
  env_file:
    - docker-env.yml

mysql:
  build: docker/mysql/
  ports:
    - 13306:3306
  env_file:
    - docker-env.yml

solr:
  build: docker/solr/
  ports:
    - 18983:8983
  env_file:
    - docker-env.yml

#elasticsearch:
#  build: docker/elasticsearch/
#  ports:
#    - 9200:9200
#    - 9300:9300
#  env_file:
#    - docker-env.yml